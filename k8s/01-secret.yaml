# NOT: Bu dosya sadece lokal/demo amaçlıdır, şifre düz metin olarak commit'leniyor.
# Gerçek bir ortamda bu dosya .gitignore'a alınır ve Secret `kubectl create secret`
# ile ya da bir secret manager (Sealed Secrets, Vault vb.) ile oluşturulur.
apiVersion: v1
kind: Secret
metadata:
  name: mongo-secret
  namespace: todo
type: Opaque
stringData:
  MONGO_INITDB_ROOT_USERNAME: todoadmin
  MONGO_INITDB_ROOT_PASSWORD: change-me-local-only
